[build]
builder = "NIXPACKS"
buildCommand = "npm run build -w @cesafiu/web"

[deploy]
startCommand = "npm run start -w @cesafiu/web -- -p ${PORT:-3000}"
healthcheckPath = "/ro"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
